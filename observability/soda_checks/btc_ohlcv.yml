checks for btc_ohlcv:
  # Table must have been refreshed within the last 10 minutes
  - freshness(ingested_at) < 10m

  # Table must not be empty
  - row_count > 0

  # No duplicate candles — open_time is the natural key
  - duplicate_count(open_time) = 0
